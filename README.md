# SPITE QTE 助手

> 基于 OpenCV 的图像识别脚本，自动完成 SPITE 游戏中的 QTE（快速反应事件）小游戏，解放双手。

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![OpenCV](https://img.shields.io/badge/OpenCV-4.x-green.svg)

---

## 📌 项目简介

本脚本通过 **屏幕实时捕捉 + HSV 颜色识别**，检测 QTE 判定条中的 **绿色安全区** 与 **白色判定线**，并在最佳时机自动按下 **空格键**，实现全自动 QTE 通关。[我实在是受不了这该死游戏的QTE了]

采用 **动态延迟算法**，根据绿色区域宽度自动调整触发时机，**绿色完美判定成功率 90%+**。

---

## 🎮 适用场景

- SPITE 内的 QTE 节奏小游戏

---

## ⚙️ 运行环境

### 系统要求

- Windows 10 / 11
- Python 3.8+

### 依赖

```bash
opencv-python numpy mss pynput
```

---

## 🚀 使用说明

- 将游戏设置为 **全屏**，此脚本基于2k分辨率实现。
- 建议使用 PyCharm 导入该项目并使用 PyCharm 启动脚本
- 启动后根据代码提示"于 5s 内切换至游戏内 QTE 游戏界面"

- 控制台会实时显示判定信息：

  ```
  竖线: 1245 | 绿色: [980, 1520] | 距离: 28
  ```

- 触发成功提示：

  ```
  触发！
     绿色宽度: 180px
     竖线: 1245 | 绿色: [980, 1520]
     距离边界: 28px
     还需1.52帧 | 动态延迟: 1.7帧
  ⌨️ 已按空格
  ```

---

## 🧠 工作原理

```text
屏幕捕获 (mss)
        ↓
HSV 色彩空间转换
        ↓
识别绿色判定区域（安全区）+ 宽度过滤
识别白色判定线（指针）
        ↓
计算白线到绿色边界的距离
        ↓
根据绿色宽度动态调整延迟帧数
        ↓
剩余帧数 ≤ 延迟阈值 → 自动按空格
```

---

## 🛠 配置参数说明

| 参数              | 说明                    | 默认值                                  |
| :---------------- | :---------------------- | :-------------------------------------- |
| `BAR`             | QTE 判定条截图区域 (2K) | `{"top": 1060, "left": 120, ...}`       |
| `GREEN_LO/HI`     | 绿色判定区 HSV 范围     | `[25, 90, 95] ~ [45, 255, 255]`         |
| `WHITE_LO/HI`     | 白色判定线 HSV 范围     | `[0, 0, 200] ~ [180, 30, 255]`          |
| `PIXELS_PER_FRAME`| 每帧移动像素数          | `31`                                    |
| `MIN_GREEN_WIDTH` | 最小绿色宽度（过滤噪声）| `50`                                    |
| `MAX_GREEN_WIDTH` | 最大绿色宽度（过滤噪声）| `500`                                   |
| `cooldown`        | 触发后冷却时间          | `0.3 秒`                                |

**动态延迟策略**：绿色宽度 ≥200px 延迟 2 帧，100-200px 延迟 1.7 帧，<100px 延迟 1.5 帧

⚠️ **不同分辨率 / 画质需自行微调参数**

---

## 🖥 分辨率适配

如果你的分辨率不是 **2560×1440 (2K)**：

1. 使用截图工具测量 QTE 条的位置。
2. 修改 `BAR` 字典中的 `top`, `left`, `width`, `height`。
3. 根据实际测试自行调整 `LEAD_PIXELS`。